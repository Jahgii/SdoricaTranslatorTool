import { CommonDictionaryDirective } from './common-dictionary.directive';

describe('CommonDictionaryDirective', () => {
  it('should create an instance', () => {
    const directive = new CommonDictionaryDirective();
    expect(directive).toBeTruthy();
  });
});
